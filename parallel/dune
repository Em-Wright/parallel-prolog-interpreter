(executable
  (public_name parallel_prolog_interpreter)
   (name worker)
    (promote (until-clean))
     (libraries async core_unix prolog_interpreter rpc_parallel_edit)
      (preprocess (pps ppx_jane))
)
